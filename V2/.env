# ============ ВАШИ НАСТРОЙКИ ============
# Внешний IP или домен, который указывает на роутер
EXTERNAL_IP=ваш.внешний.ip
# или если есть домен:
# EXTERNAL_IP=mqtt.ваш-домен.ru

# Пароль для root-токена Vault (сгенерировать случайный)
VAULT_SECRET=$(openssl rand -base64 32 | cut -c1-24)

# Внутренний IP Сервера 1 (автоматически определится)
SERVER1_IP=$(hostname -I | awk '{print $1}')